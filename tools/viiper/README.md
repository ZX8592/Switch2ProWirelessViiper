Place VIIPER for local release builds in this folder.

Expected files:

- `viiper.exe`
- `VIIPER_LICENSES.txt`

`viiper.exe` is ignored by Git. The publish script copies these files into the
published app folder when they are present.
